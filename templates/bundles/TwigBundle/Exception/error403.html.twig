{# templates/bundles/TwigBundle/Exception/error403.html.twig #}
{% extends 'base.html.twig' %}

{% block title %}Accès interdit{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <style>
        .error-container {
            text-align: center;
            padding: 50px 20px;
            max-width: 800px;
            margin: 0 auto;
        }
        
        .error-title {
            font-size: 10rem;
            color: #dc3545;
            margin-top: 0px;
            margin-bottom: 0px;
        }
        
        .error-subtitle {
            font-size: 2rem;
            color: #333;
            margin-top: 0;
        }
        
        .error-text {
            font-size: 1.2rem;
            margin: 30px 0;
            color: #666;
        }
        
        .error-image {
            max-width: 400px;
            margin: 20px 0;
        }
        
        .home-button {
            display: inline-block;
            background-color: #7A36DB;
            color: white;
            padding: 12px 24px;
            border-radius: 5px;
            text-decoration: none;
            font-weight: bold;
            transition: background-color 0.3s;
            margin-right: 10px;
        }
        
        .home-button:hover {
            background-color: #5e2ab4;
        }
        
        .login-button {
            display: inline-block;
            background-color: #28a745;
            color: white;
            padding: 12px 24px;
            border-radius: 5px;
            text-decoration: none;
            font-weight: bold;
            transition: background-color 0.3s;
        }
        
        .login-button:hover {
            background-color: #218838;
        }
    </style>
{% endblock %}

{% block body %}
<div class="error-container">
    <h1 class="error-title">403</h1>
    <h2 class="error-subtitle">Accès interdit</h2>
    
    <p class="error-text">
        Halte ! Même avec le Sharingan ou le Haki de l'observation, 
        vous ne pouvez pas accéder à cette page ! Vous n'avez pas les permissions nécessaires.
    </p>
    
    <img src="{{ asset('images/errors/403.webp') }}" alt="fille bloquant l'accès" class="error-image">
    
    <p class="error-text">
        Connectez-vous ou retournez à la page d'accueil pour continuer votre exploration.
    </p>
    
    <div>
        <a href="{{ path('app_home') }}" class="home-button">Retour à l'accueil</a>
        {% if not is_granted('IS_AUTHENTICATED_FULLY') %}
            <a href="{{ path('app_login') }}" class="login-button">Se connecter</a>
        {% endif %}
    </div>
</div>
{% endblock %}